% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generic-summaries.R
\name{residual_diagnostics}
\alias{residual_diagnostics}
\title{Residual diagnostics using DHARMa}
\usage{
residual_diagnostics(x, n = 250)
}
\arguments{
\item{x}{A model fitted in seqwrap, or NULL when fitting failed.}

\item{n}{Number of simulations passed to \code{DHARMa::simulateResiduals()}.}
}
\value{
A one row tibble of p-values, or NULL when \code{x} is NULL.
}
\description{
Simulates scaled residuals with DHARMa and tests them for uniformity,
dispersion and outliers. This was the behaviour of \code{generic_evaluation()} in
seqwrap 0.7.0 and earlier, and is kept here as an opt-in evaluation function.
}
\details{
Simulating residuals is far more expensive than reading a convergence code:
each target is refit-equivalent \code{n} times. On data sets with many thousands
of targets this dominates the total run time, which is why it is no longer
the default. Consider applying it to a subset of targets, or lowering \code{n}.

DHARMa is an optional dependency, so it has to be installed before this
function can be used.
}
\examples{
library(seqwrap)

if (requireNamespace("DHARMa", quietly = TRUE)) {
  fit <- stats::glm(mpg ~ wt, data = mtcars, family = stats::gaussian())
  residual_diagnostics(fit, n = 50)
}

}
