% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seqwrap-priors.R
\name{dispersion_evaluation}
\alias{dispersion_evaluation}
\title{Dispersion and mean count from a glmmTMB model}
\usage{
dispersion_evaluation(x, ...)
}
\arguments{
\item{x}{A model fitted with \code{glmmTMB::glmmTMB}, or NULL when fitting
failed.}

\item{...}{Currently unused.}
}
\value{
A one row tibble, or NULL when \code{x} is NULL, with columns:
\describe{
\item{dispersion}{The dispersion parameter on the log scale, as estimated
by \code{glmmTMB}. For \code{nbinom2} this is \code{log(theta)}, which equals
\code{log(sigma(x))}.}
\item{dispersion.se}{The standard error of \code{dispersion}, NA when
\code{glmmTMB} did not compute one.}
\item{log_mu}{The natural logarithm of the mean of the observed response
values used to fit the model.}
}
}
\description{
An evaluation function for \code{eval_fun} in \code{\link[=seqwrap]{seqwrap()}} and
\code{\link[=seqwrap_compose]{seqwrap_compose()}} that records the estimated dispersion of a negative
binomial \code{glmmTMB} model together with the mean of the observed counts. It
provides the per-target quantities that \code{\link[=seqwrap_priors]{seqwrap_priors()}} uses to build a
dispersion prior that depends on the expression level of each target.
}
\details{
The dispersion is read from the \code{betadisp} element of the model's \code{sdreport}.
When a dispersion formula contains covariates only its first element, the
intercept, is reported. The convergence diagnostics returned by
\code{\link[=generic_evaluation]{generic_evaluation()}} are not included; to keep both, wrap the two in a
function that binds their results.
}
\examples{
library(seqwrap)

if (requireNamespace("glmmTMB", quietly = TRUE)) {
  library(glmmTMB)

  dat <- simcounts(n_genes = 10, n_samples = 30)

  counts <- dat$data
  metadata <- dat$metadata
  metadata$ln_libsize <- log(colSums(counts[, -1]))
  metadata$y <- as.integer(counts[1, -1])

  m <- glmmTMB(y ~ x + offset(ln_libsize),
               data = metadata,
               family = nbinom2)

  dispersion_evaluation(m)

  # Equivalent to the dispersion reported by sigma() on the log scale
  log(sigma(m))
}

}
\seealso{
\code{\link[=seqwrap_priors]{seqwrap_priors()}}, which consumes the columns returned here.
}
