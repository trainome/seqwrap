% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seqwrap-priors.R
\name{seqwrap_priors}
\alias{seqwrap_priors}
\title{Build empirical Bayes priors for glmmTMB from a seqwrap run}
\usage{
seqwrap_priors(
  x,
  data,
  rownames = FALSE,
  terms = NULL,
  center = c("zero", "mean"),
  robust = FALSE,
  ranef = TRUE,
  shape = 2,
  dispersion = "dispersion",
  trend = c("loess", "constant"),
  span = 0.75,
  drop_warnings = FALSE,
  digits = 3,
  plot = FALSE
)
}
\arguments{
\item{x}{A seqwrapResults object from a run without priors, or the list
returned by \code{\link[=seqwrap_summarise]{seqwrap_summarise()}} for that run.}

\item{data}{The target data frame given to \code{\link[=seqwrap_compose]{seqwrap_compose()}}, with target
identifiers in the first column (or in the row names when
\code{rownames = TRUE}) and one column per sample. It determines the targets
for which priors are built, their order, and the mean count of each target.
When \code{\link[=seqwrap_compose]{seqwrap_compose()}} was given a list of data frames, pass the element
holding the counts.}

\item{rownames}{Logical, are target identifiers held in the row names of
\code{data}? Defaults to FALSE, as in \code{\link[=seqwrap_compose]{seqwrap_compose()}}.}

\item{terms}{Character vector naming the fixed-effect terms to place priors
on. NULL (the default) uses every fixed-effect term in the summaries except
the intercept. The intercept can be included by naming it, \code{"(Intercept)"},
which is sensible only with \code{center = "mean"}.}

\item{center}{Where to center the fixed-effect priors. \code{"zero"} (the
default) shrinks estimates towards no effect, using \code{normal(0, s)} with \code{s}
the spread of the estimates across targets. \code{"mean"} centers each prior on
the average estimate across targets, \code{normal(m, s)}.}

\item{robust}{Logical. When TRUE, the median and median absolute deviation
replace the mean and standard deviation when summarising estimates, and the
dispersion trend is fitted with a robust loss. This limits the influence
of targets whose fits went astray. Defaults to FALSE.}

\item{ranef}{Logical, should priors be placed on random-effect standard
deviations? Defaults to TRUE. Ignored when the summaries contain no random
effect terms. A grouping variable whose standard deviation estimates are
essentially zero across targets is skipped with a warning, since the
gamma prior needs a positive mean.}

\item{shape}{The shape of the gamma prior placed on random-effect standard
deviations. \code{glmmTMB} parameterises this prior by its mean and shape, and
the mean is taken from the estimates across targets. Defaults to 2.}

\item{dispersion}{The name of the column in the evaluations that holds the
log dispersion of each target, as produced by \code{\link[=dispersion_evaluation]{dispersion_evaluation()}}.
Defaults to \code{"dispersion"}. Set to NULL to build no dispersion prior.}

\item{trend}{How the dispersion prior depends on expression level.
\code{"loess"} (the default) fits a loess curve of log dispersion against log
mean count and gives every target a prior centered on the curve at its own
mean count, with the residual standard error of the curve as prior
standard deviation. \code{"constant"} gives every target the same prior,
centered on the average log dispersion across targets.}

\item{span}{The span of the loess fit when \code{trend = "loess"}. Defaults to
0.75.}

\item{drop_warnings}{Passed to \code{\link[=seqwrap_summarise]{seqwrap_summarise()}} when \code{x} is a
seqwrapResults object, to exclude targets that raised warnings from the
prior estimation. Defaults to FALSE. Ignored when \code{x} is already a list of
combined results.}

\item{digits}{The number of decimals kept in the prior specifications.
Defaults to 3.}

\item{plot}{Logical, should the priors be plotted over the estimates they
were built from before the object is returned? Defaults to FALSE. See
\code{\link[=plot.seqwrap_priors]{plot.seqwrap_priors()}}, which can also be called on the result later.}
}
\value{
A named list of class \code{seqwrap_priors} with one element per row of
\code{data}, in the same order and named by target identifier. Each element is
a data frame with the columns \code{prior}, \code{class} and \code{coef} that
\code{glmmTMB::glmmTMB} expects in its \code{priors} argument. The list carries
attributes that describe how it was built: \code{common}, a data frame of the
priors shared by all targets with their location and scale;
\code{dispersion}, a data frame with the log mean count and prior location of
each target, or NULL; \code{trend}, the fitted loess model, or NULL; \code{n},
the number of targets that contributed to the estimation; and \code{data},
the per-target estimates the priors were built from, see below.
}
\description{
Summarises the parameter estimates from a completed \code{\link[=seqwrap]{seqwrap()}} run and
turns them into target-specific prior specifications for
\code{glmmTMB::glmmTMB}. The result is a list with one prior data frame per
target, ready to be passed as \code{targetdata} to \code{\link[=seqwrap_compose]{seqwrap_compose()}} for a
second, regularised run. This is a simple empirical Bayes strategy: every
target is first fitted on its own, the spread of each parameter across
targets then becomes the prior for that parameter.
}
\details{
Three kinds of prior are built.
\itemize{
\item Fixed effects (class \code{"fixef"}). For each term the estimates across
targets are summarised by their location and spread, giving a normal prior
\code{normal(location, spread)}. Terms are identified from the \code{effect} and
\code{component} columns of a \code{\link[=generic_summary]{generic_summary()}} result. With a custom summary
function that lacks these columns, every term other than the intercept and
terms starting with \code{sd__} or \code{cor__} is treated as a fixed effect.
\item Random-effect standard deviations (class \code{"ranef_sd"}). For each grouping
variable the standard deviation estimates across targets are averaged and
used as the mean of a gamma prior, \code{gamma(mean, shape)}, on the standard
deviation scale. A grouping variable with several standard deviation terms
receives one prior for all of them.
\item Dispersion (class \code{"fixef_disp"}). The log dispersion of each target
depends strongly on its expression level, so a trend of log dispersion
against log mean count is estimated and each target receives a normal
prior centered on the trend at its own mean count. Targets whose mean count
lies outside the range seen during estimation, including targets with zero
counts, receive the prior at the nearest end of that range.
}

The spread of estimates across targets includes the estimation error of
each estimate, so the resulting priors are somewhat wider than the true
between-target variation, which makes them conservative.

The container for the second run must ask for the priors by name, using
\code{alist()} so that the data frame is built on the worker for each target:

\if{html}{\out{<div class="sourceCode">}}\preformatted{arguments = alist(
  formula = ...,
  family = glmmTMB::nbinom2,
  priors = data.frame(prior = prior, class = class, coef = coef)
)
}\if{html}{\out{</div>}}

Priors are experimental in \code{glmmTMB}; see its \code{priors} help page and
vignette for the parameterisation of each distribution.
}
\section{Estimates used to build the priors}{

The \code{data} attribute holds the estimates that went into each prior, so that
the priors can be inspected and plotted with other tools than
\code{\link[=plot.seqwrap_priors]{plot.seqwrap_priors()}}. It is a list with two elements.
\itemize{
\item \code{estimates}, a data frame with one row per target and prior, with the
columns \code{target}, \code{class} (\code{"fixef"} or \code{"ranef_sd"}), \code{coef} and
\code{estimate}. For fixed effects \code{estimate} is the coefficient of the term
in the target's model. For random effects it is the estimated standard
deviation, one row per standard deviation term within the grouping
variable.
\item \code{dispersion}, a data frame with the columns \code{target}, \code{log_mu} and
\code{dispersion} holding the log mean count and the estimated log dispersion
of every target used to estimate the dispersion trend, or NULL when no
dispersion prior was built.
}

The location and scale of each prior is found in the \code{common} and
\code{dispersion} attributes, and the loess fit in \code{trend}.
}

\examples{
library(seqwrap)

if (requireNamespace("glmmTMB", quietly = TRUE)) {
  dat <- simcounts(n_genes = 20, n_samples = 30, clusters = 15)
  counts <- dat$data
  metadata <- dat$metadata
  metadata$ln_libsize <- log(colSums(counts[, -1]))

  # First run, without priors, recording the dispersion of each target
  container <- seqwrap_compose(
    modelfun = glmmTMB::glmmTMB,
    arguments = list(
      formula = y ~ x + (1 | cluster) + offset(ln_libsize),
      family = glmmTMB::nbinom2
    ),
    data = counts,
    metadata = metadata,
    samplename = "sample",
    eval_fun = dispersion_evaluation
  )

  results <- seqwrap(container, cores = 1, verbose = FALSE)

  # Priors pooled across targets, one data frame per target
  priors <- seqwrap_priors(results, data = counts)
  priors
  priors[[1]]

  \donttest{
  # Second run, with the priors passed as target-specific data
  container_prior <- seqwrap_compose(
    modelfun = glmmTMB::glmmTMB,
    arguments = alist(
      formula = y ~ x + (1 | cluster) + offset(ln_libsize),
      family = glmmTMB::nbinom2,
      priors = data.frame(prior = prior, class = class, coef = coef)
    ),
    data = counts,
    metadata = metadata,
    samplename = "sample",
    targetdata = priors
  )

  results_prior <- seqwrap(container_prior, subset = 1:5,
                           return_models = TRUE, cores = 1,
                           verbose = FALSE)

  # The fitted model carries the priors it was given
  results_prior@models[[1]]$modelInfo$priors
  }
}

}
\seealso{
\code{\link[=dispersion_evaluation]{dispersion_evaluation()}} to record the dispersion during the first
run, and \code{\link[=seqwrap_compose]{seqwrap_compose()}} for the \code{targetdata} argument.
}
